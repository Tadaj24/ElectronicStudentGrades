import users.student.StudentImpl;
import users.teacher.TeacherImpl;

import java.util.List;

public class GradeSystem {
    List<StudentImpl> studentList = Temp.getSimpleStudents();
    //List<TeacherImpl> teacherList;
}
